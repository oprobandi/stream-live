{
  "name": "stream-live",
  "version": "1.2.0",
  "description": "Free-first video platform. Browse and watch without friction. Premium unlocks only when you choose it.",
  "homepage": "https://your-github-username.github.io/stream-live",
  "private": true,
  "scripts": {
    "start": "npx serve . --listen 3000",
    "predeploy": "git config user.email \"deploy@streamlive.io\" && git config user.name \"Stream Live\" && echo 'Deploying Stream Live to GitHub Pages…'",
    "deploy": "gh-pages -d . --dotfiles --no-history"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "serve": "^14.2.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-github-username/stream-live.git"
  },
  "keywords": ["video", "streaming", "platform", "free-first"],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
